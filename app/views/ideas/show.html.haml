%h1= @idea.title
%p= @idea.description
%p= formatted_date(@idea.created_at)

= form_for @comment, url: idea_comments_path(@idea) do |i|
  = i.text_area :body
  %br
  = i.submit "Submit a comment", class: "btn btn-primary"
%hr
= render @comments